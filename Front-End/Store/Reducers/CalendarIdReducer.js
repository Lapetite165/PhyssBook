const initialState = {calendarId:[]}

export default function calendarIdReducer(state=initialState, action){
    let nextState

    switch (action.type) {
        case 'CALENDAR_ID':
            
        default :
            return state
    }
}